import 'package:flutter_test/flutter_test.dart';
import 'package:feeltrip_app/services/auth_service.dart';

void main() {
  group('AuthService', () {
    test('placeholder - Firebase requiere dispositivo real', () {
      // Firebase.initializeApp() requiere plataforma nativa
      // Tests de integración se ejecutan con: flutter test integration_test/
      expect(true, isTrue);
    });
  });
}
